package ${package};

import ${package}.param.${className}Input;
import ${package}.param.${className}Output;

/**
* @author auto
* @version 1.0
*/
@FunctionalInterface
public interface ${className} {

    ${className}Output execute(${className}Input input);
}